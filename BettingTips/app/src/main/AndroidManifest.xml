<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pandurski.bettingtips">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/betting_tips_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/betting_tips_launcher_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".todays_tips">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".yesterday_tips"></activity>
    </application>

</manifest>